id: no-cross-module-coupling
description: "Billing must not import notifications directly"
why: "Cross-module coupling makes billing depend on notification internals."

scope: [ci]
enforcement: block
weight: 4

match:
  type: structural
  forbidden:
    - from: "**/billing/**"
      to: "**/notifications/**"
